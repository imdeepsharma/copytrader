export BSC_RPC_URL="https://bsc-dataseed.binance.org/"
export TARGET_WALLET="0xTargetAddressToMirror"
export PRIVATE_KEY="0xyourprivatekey"              # NEVER hardcode; use env/secret manager
export SLIPPAGE_BPS="100"                          # 100 = 1% slippage
export POLL_SECONDS="7"                            # 5–10 is typical
export SIMULATION_MODE="1"                         # 1 = simulate (no txs), 0 = live
# Sizing: choose ONE (fallback: multiplier if both set)
export AMOUNT_MULTIPLIER_BP="1000"                 # 1000 bp = 10% of target input
# OR fixed native (BNB) amount when the source is BNB/WBNB:
export FIXED_INPUT_BNB="0.02"
